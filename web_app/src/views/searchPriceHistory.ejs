<%- include('partials/_header'); %>

<section class="header">
    <div id="searchitemPanel">
        <h1>Histórico de precios de vehículos</h1>
        <table id="articleTable" class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>VALOR UNITARIO INSERTADO</th>
                    <th>VALOR UNITARIO REEMPLAZADO</th>                                
                    <th>FECHA DE MODIFICACIÓN</th>
                    <th>VEHICULO ASOCIADO</th>                                     
                </tr>
            </thead>
            <tbody>
                <% if (data) { %>
                    <% for (var i = 0; i < data.length; i++) { %>
                    <tr>
                        <td><%= data[i].ID_HISTORICO %></td>
                        <td><%= data[i].VALOR_UNITARIO_INSERTADO %></td>
                        <td><%= data[i].VALOR_UNITARIO_REEMPLAZADO %></td>
                        <td><%= data[i].FECHA_MODIFICACION %></td>
                        <td><%= data[i].VEHICULOS_ID_VEHICULOS %></td>
                    </tr>
                    <% } %>
                <% } %>
            </tbody>
        </table>
    </div>
</section>

<%- include('partials/_footer'); %>